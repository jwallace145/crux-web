# Environment Configuration Example
# Copy this file to .env.local and configure for your environment
# NOTE: .env.local is gitignored and should NOT be committed

# API Configuration
# ================

# NEXT_PUBLIC_API_URL (Optional)
# Full URL to the backend API. Takes precedence over environment-based URL construction.
#
# For local development with Go Fiber backend:
# NEXT_PUBLIC_API_URL=http://localhost:3000
#
# For testing against a deployed environment:
# NEXT_PUBLIC_API_URL=http://dev-api.cruxproject.io
# NEXT_PUBLIC_API_URL=http://staging-api.cruxproject.io
# NEXT_PUBLIC_API_URL=http://prod-api.cruxproject.io

# NEXT_PUBLIC_API_ENVIRONMENT (Default: "dev")
# Environment name used to construct the API URL when NEXT_PUBLIC_API_URL is not set.
# In production builds, the URL becomes: http://{environment}-api.cruxproject.io
# Common values: dev, staging, prod
NEXT_PUBLIC_API_ENVIRONMENT=dev

# Behavior Summary:
# ================
# 1. If NEXT_PUBLIC_API_URL is set ’ uses that URL exactly
# 2. If NODE_ENV=development (npm run dev) ’ defaults to http://localhost:3000
# 3. If in production build ’ uses http://{NEXT_PUBLIC_API_ENVIRONMENT}-api.cruxproject.io
